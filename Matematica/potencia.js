// Calcular a potência de um número

const potencia = (numero, expoente) => {
    return numero ** expoente
};

let numero = potencia(5, 2)

console.log(numero);

/*Para calcular a potencia, basta inserir o número escolhido como primeiro 
parâmetro e o índice como segundo*/